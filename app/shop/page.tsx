import {products} from '@/lib/catalog';import {CatalogCard} from '@/components/CatalogCard';
export const dynamic='force-dynamic';
export default async function Shop(){const items=await products();return <section className="page wrap"><div className="page-intro"><p className="eyebrow">THE COLLECTION</p><h1>Objects with a little magic.</h1><p>Small-batch original art, handmade pieces, and thoughtful details from the studio.</p></div><div className="toolbar"><input aria-label="Search products" placeholder="Search the shop"/><select aria-label="Sort products"><option>Newest first</option><option>Price: low to high</option></select></div><div className="filter-row"><button className="chip">All pieces</button></div>{items.length?<div className="product-grid">{items.map(product=><CatalogCard product={product} key={product.id}/>)}</div>:<div className="empty">The shop is being prepared. Please check back soon.</div>}</section>}
