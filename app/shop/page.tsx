'use client';import {useState} from 'react';import {displayProducts} from '@/lib/store';import {ProductCard} from '@/components/ProductCard';
export default function Shop(){const [q,setQ]=useState('');const items=displayProducts.filter(p=>p.name.toLowerCase().includes(q.toLowerCase())||p.category.toLowerCase().includes(q.toLowerCase()));return <section className="page wrap"><div className="page-intro"><p className="eyebrow">THE COLLECTION</p><h1>Objects with a little magic.</h1><p>Small-batch original art, handmade pieces, and thoughtful details from the studio.</p></div><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} aria-label="Search products" placeholder="Search the shop"/><select aria-label="Sort products"><option>Newest first</option><option>Price: low to high</option></select></div><div className="filter-row">{['All','Art Prints','Handmade','Accessories','Custom','Commission'].map(x=><button className="chip" key={x}>{x}</button>)}</div><div className="product-grid">{items.map(p=><ProductCard p={p} key={p.id}/>)}</div></section>}
