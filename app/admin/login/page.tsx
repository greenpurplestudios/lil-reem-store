'use client';import {useState} from 'react';import {createSupabaseBrowserClient} from '@/lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const {error}=await createSupabaseBrowserClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/admin`}});setMessage(error?error.message:'Check your email for a secure sign-in link.')};return <section className="page wrap"><div className="page-intro"><p className="eyebrow">STUDIO ADMIN</p><h1>Sign in</h1><p>Use the approved studio account to manage LIL REEM STORE.</p></div><form className="form" onSubmit={submit}><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="button">Send secure sign-in link</button>{message&&<div className="notice">{message}</div>}</form></section>}
