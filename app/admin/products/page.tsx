'use client';import {useEffect,useState} from 'react';import {createSupabaseBrowserClient} from '@/lib/supabase';import {AdminNav} from '@/components/AdminNav';
type Item={id:string;name:string;slug:string;price_cents:number;stock_quantity:number;status:string;featured:boolean};const slugify=(x:string)=>x.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');
export default function Products(){const [items,setItems]=useState<Item[]>([]),[message,setMessage]=useState(''),[saving,setSaving]=useState(false);const load=async()=>{const {data}=await createSupabaseBrowserClient().from('products').select('id,name,slug,price_cents,stock_quantity,status,featured').order('created_at',{ascending:false});setItems(data??[])};useEffect(()=>{load()},[]);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget),name=String(f.get('name'));const {error}=await createSupabaseBrowserClient().from('products').insert({name,slug:slugify(name),description:f.get('description'),price_cents:Math.round(Number(f.get('price'))*100),stock_quantity:Number(f.get('stock')),status:f.get('status'),featured:f.get('featured')==='on'});setSaving(false);setMessage(error?error.message:'Product saved.');if(!error){e.currentTarget.reset();load()}};const update=async(item:Item,status:string)=>{await createSupabaseBrowserClient().from('products').update({status}).eq('id',item.id);load()};return <section className="admin wrap"><div className="admin-grid"><AdminNav/><div><p className="eyebrow">PRODUCTS</p><h1>Collection</h1><form className="form admin-card" onSubmit={submit}><h2>Add a product</h2><div className="split"><label>Name<input name="name" required/></label><label>Price (USD)<input name="price" type="number" min="0" step="0.01" required/></label></div><label>Description<textarea name="description" required/></label><div className="split"><label>Stock<input name="stock" type="number" min="0" defaultValue="1" required/></label><label>Status<select name="status"><option value="published">Published</option><option value="draft">Draft</option><option value="hidden">Hidden</option><option value="sold_out">Sold out</option></select></label></div><label><input name="featured" type="checkbox"/> Feature on homepage</label><button className="button" disabled={saving}>{saving?'Saving…':'Publish product'}</button>{message&&<p className="notice">{message}</p>}</form><div className="admin-card"><h2>Current products</h2>{items.length?items.map(item=><div className="admin-row" key={item.id}><div><b>{item.name}</b><small>${(item.price_cents/100).toFixed(2)} · {item.stock_quantity} in stock · {item.status}</small></div><div><button className="chip" onClick={()=>update(item,item.status==='hidden'?'published':'hidden')}>{item.status==='hidden'?'Show':'Hide'}</button><button className="chip" onClick={()=>update(item,'sold_out')}>Sold out</button></div></div>):<p className="empty">No products yet.</p>}</div></div></div></section>}
