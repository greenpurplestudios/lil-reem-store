import Image from 'next/image';import Link from 'next/link';import {ShoppingBag} from 'lucide-react';import {Product,money} from '@/lib/store';
export function ProductCard({p}: {p:Product}){return <article className="product-card"><Link href={'/product/'+p.id}><div className="product-image"><Image src={p.image} alt={p.name} fill sizes="(max-width:700px) 50vw, 25vw"/></div></Link><div className="product-meta"><span>{p.category}</span><h3>{p.name}</h3><div><b>{money(p.price)}</b><button aria-label={'Add '+p.name+' to cart'}><ShoppingBag size={17}/></button></div></div></article>}
