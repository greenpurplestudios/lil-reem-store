import Image from 'next/image';import Link from 'next/link';import {ShoppingBag} from 'lucide-react';import {imageUrl,type Product} from '@/lib/catalog';
export function CatalogCard({product}:{product:Product}){return <article className="product-card"><Link href={`/product/${product.slug}`}><div className="product-image"><Image fill sizes="(max-width:700px) 50vw, 25vw" src={imageUrl(product.product_images[0]?.storage_path)} alt={product.product_images[0]?.alt_text||product.name}/></div></Link><div className="product-meta"><span>{product.categories?.name||'Studio item'}</span><h3>{product.name}</h3><div><b>{new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(product.price_cents/100)}</b><Link aria-label={`View ${product.name}`} href={`/product/${product.slug}`}><ShoppingBag size={17}/></Link></div></div></article>}
